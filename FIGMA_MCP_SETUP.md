# Figma MCP 연결 가이드

## 🎨 Figma MCP란?

Figma MCP는 Cursor에서 Figma 디자인 파일에 직접 접근하고 작업할 수 있게 해주는 Model Context Protocol 서버입니다.

**할 수 있는 것:**
- Figma 파일 내용 조회
- 디자인 컴포넌트 정보 가져오기
- 색상, 텍스트 스타일 추출
- 레이어 구조 파악

---

## 🔑 1단계: Figma Access Token 발급

### Figma에서 토큰 생성:

1. [Figma Settings](https://www.figma.com/settings) 접속
2. 계정 설정 페이지에서 **"Personal Access Tokens"** 섹션 찾기
3. **"Generate new token"** 클릭
4. 토큰 이름 입력 (예: "Cursor MCP")
5. **생성된 토큰 복사** (다시 볼 수 없으니 주의!)

**토큰 형식:**
```
figd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

⚠️ **주의:** 이 토큰은 비밀번호처럼 안전하게 보관하세요!

---

## ⚙️ 2단계: MCP 설정 업데이트

현재 MCP 설정 파일(`~/.cursor/mcp_settings.json`)에 Figma가 추가되었습니다:

```json
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "supabase-mcp"],
      "env": {
        "SUPABASE_URL": "...",
        "SUPABASE_KEY": "..."
      }
    },
    "figma": {
      "command": "npx",
      "args": ["-y", "figma-mcp"],
      "env": {
        "FIGMA_ACCESS_TOKEN": "YOUR_FIGMA_ACCESS_TOKEN"
      }
    }
  }
}
```

### 토큰 입력하기:

**방법 1: 터미널에서 (권장)**

```bash
# 설정 파일 열기
open -a TextEdit ~/.cursor/mcp_settings.json

# 또는 nano 에디터로
nano ~/.cursor/mcp_settings.json
```

`YOUR_FIGMA_ACCESS_TOKEN`을 실제 토큰으로 교체:
```json
"FIGMA_ACCESS_TOKEN": "figd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
```

**방법 2: 직접 명령어로**

```bash
# 토큰을 환경 변수로 설정
export FIGMA_TOKEN="figd_your_actual_token_here"

# 설정 파일 업데이트
cat > ~/.cursor/mcp_settings.json << EOF
{
  "mcpServers": {
    "supabase": { ... },
    "figma": {
      "command": "npx",
      "args": ["-y", "figma-mcp"],
      "env": {
        "FIGMA_ACCESS_TOKEN": "$FIGMA_TOKEN"
      }
    }
  }
}
EOF
```

---

## 🔄 3단계: Cursor 재시작

설정을 저장한 후 **Cursor를 완전히 종료하고 다시 시작**하세요:

1. Cursor 종료: `Cmd + Q` (Mac) 또는 완전 종료
2. Cursor 다시 실행

---

## 🧪 4단계: 테스트

### Figma MCP가 작동하는지 확인:

Cursor에서 저에게 다음과 같이 요청해보세요:

**예시 요청:**
```
"Figma에서 [파일명] 가져와줘"
"Figma 디자인의 색상 팔레트를 보여줘"
"Figma 컴포넌트 목록을 가져와줘"
```

---

## 🎯 사용 예시

### 1. Figma 파일 정보 조회

```
저: "Figma에서 babyshop 디자인 파일 내용 가져와줘"

→ Figma MCP가 파일 구조, 페이지, 프레임 정보 제공
```

### 2. 색상 팔레트 추출

```
저: "Figma 디자인에서 사용된 모든 색상 코드 알려줘"

→ 디자인에 사용된 색상 목록과 HEX 코드 제공
```

### 3. 컴포넌트 구조 파악

```
저: "Figma의 버튼 컴포넌트 스타일 정보 가져와줘"

→ 버튼의 크기, 색상, 텍스트 스타일 등 상세 정보
```

### 4. 디자인 → 코드 변환

```
저: "이 Figma 프레임을 React 컴포넌트로 만들어줘"

→ Figma 디자인을 분석하여 React 코드 생성
```

---

## 📁 Figma 파일 URL 형식

Figma 파일에 접근하려면 **파일 URL 또는 File Key**가 필요합니다:

**URL 형식:**
```
https://www.figma.com/file/FILE_KEY/FILE_NAME
```

**File Key 추출:**
```
https://www.figma.com/file/ABC123XYZ/My-Design
                            ↑
                      이 부분이 File Key
```

---

## 🔒 보안

### Figma Access Token 관리:

✅ **안전한 관리:**
- MCP 설정 파일에만 저장
- Git에 커밋하지 않음
- 주기적으로 토큰 갱신

❌ **절대 하지 말 것:**
- 코드에 직접 하드코딩
- GitHub에 공개
- 다른 사람과 공유

### 토큰 권한:

Figma Access Token은 다음 권한을 가집니다:
- ✅ 본인이 접근 가능한 모든 Figma 파일 조회
- ✅ 파일 내용, 스타일, 컴포넌트 읽기
- ❌ 파일 수정 불가 (읽기 전용)

---

## ⚠️ 문제 해결

### 문제 1: "Access Token is invalid"

**원인:** 토큰이 잘못되었거나 만료됨

**해결:**
1. Figma에서 새 토큰 생성
2. MCP 설정 파일 업데이트
3. Cursor 재시작

### 문제 2: "File not found"

**원인:** 
- 파일 URL이 잘못됨
- 파일에 접근 권한 없음

**해결:**
1. Figma 파일 URL 확인
2. 본인 계정으로 해당 파일 접근 가능한지 확인
3. 팀 파일의 경우 권한 요청

### 문제 3: MCP 서버가 시작 안 됨

**원인:** npm/npx 설치 안 됨

**해결:**
```bash
# Node.js 버전 확인
node --version
npm --version

# 없으면 Node.js 설치
# https://nodejs.org/
```

---

## 📚 Figma MCP 명령어

Cursor에서 사용 가능한 Figma 관련 요청:

### 파일 조회:
- "Figma 파일 [FILE_KEY] 구조 보여줘"
- "Figma에서 [페이지명] 페이지 정보 가져와줘"

### 스타일 추출:
- "Figma 디자인의 색상 팔레트 알려줘"
- "텍스트 스타일 정보 가져와줘"
- "버튼 컴포넌트 스타일 추출해줘"

### 코드 생성:
- "이 Figma 프레임을 React 컴포넌트로 만들어줘"
- "Figma 디자인 시스템을 CSS 변수로 변환해줘"

---

## 🎨 Figma + Babyshop 프로젝트

### 실전 활용:

1. **디자인 시스템 동기화:**
   ```
   "Figma의 색상 팔레트를 constants.ts에 반영해줘"
   ```

2. **컴포넌트 개발:**
   ```
   "Figma의 ProductCard 디자인을 React 컴포넌트로 만들어줘"
   ```

3. **스타일 추출:**
   ```
   "Figma의 타이포그래피 스타일을 Tailwind config로 변환해줘"
   ```

---

## ✅ 설정 완료 체크리스트

- [ ] Figma Access Token 발급받음
- [ ] `~/.cursor/mcp_settings.json`에 토큰 입력
- [ ] Cursor 재시작함
- [ ] Figma 파일 URL 준비됨
- [ ] 테스트 요청해봄

---

## 🔗 참고 자료

- [Figma API 문서](https://www.figma.com/developers/api)
- [Figma MCP GitHub](https://github.com/modelcontextprotocol/servers)
- [MCP 공식 문서](https://modelcontextprotocol.io)

---

## 💡 팁

### 효율적인 사용:

1. **디자인 파일 즐겨찾기:**
   - 자주 사용하는 파일 URL을 프로젝트 README에 저장

2. **색상/스타일 자동 동기화:**
   - Figma 디자인 업데이트 시 자동으로 코드 반영

3. **컴포넌트 일관성:**
   - Figma 디자인과 코드 컴포넌트 1:1 매칭

---

토큰을 발급받으셨으면 알려주세요! 설정을 완료해드리겠습니다. 🎨
